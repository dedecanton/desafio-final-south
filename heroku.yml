
build:
  nginx:
      dockerfile: Dockerfile
      worker: ./nginx

  api:
    build:
      dockerfile: Dockerfile
      worker: "./server"

  client:
    build:
      dockerfile: Dockerfile
      worker: ./frontend